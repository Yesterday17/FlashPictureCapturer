name: fpc
description: QQ/TIM FlashPicture Getter.

version: 1.0.0+2

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_local_notifications: ^0.5.2
  path_provider: ^0.5.0+1
  permission_handler: ^3.0.0
  shared_preferences: ^0.5.1+2
  fluttertoast: ^3.0.3
  open_file: 2.0.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
